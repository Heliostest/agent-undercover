# 本项目不用环境变量配 API Key：Key 在页面「模型设置」里填，
# 随 POST /api/games 的请求体进入该局 session 内存，不写盘、不写日志。
# 下面这一项是唯一的服务端开关，不设也能正常玩。

# 设为 true 才开放上帝视角接口 GET /api/games/<gameId>/reveal 与 /lab 实验入口
ENABLE_GOD_VIEW=false
